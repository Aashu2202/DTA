name: Deploy DTA Project

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          printf "%s" "${{ secrets.EC2_SSH_KEY }}" | tr -d '\r' > ~/.ssh/dta_key
          chmod 600 ~/.ssh/dta_key
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Test SSH connection
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/dta_key ubuntu@${{ secrets.EC2_HOST }} "echo SSH connected"

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/dta_key ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            set -e

            APP_DIR="/home/ubuntu/DTA"

            echo "Moving to project directory..."
            cd "$APP_DIR"

            echo "Pulling latest code..."
            git fetch origin
            git reset --hard origin/main

            echo "Updating backend..."
            cd Backend

            if [ ! -d "venv" ]; then
              python3 -m venv venv
            fi

            source venv/bin/activate
            pip install --upgrade pip
            pip install -r requirements.txt
            pip install gunicorn uvicorn

            echo "Restarting backend service..."
            sudo systemctl restart dta-backend

            echo "Building frontend..."
            cd ../Frontend
            npm install
            npm run build

            echo "Deploying frontend build..."
            sudo rm -rf /var/www/html/*
            sudo cp -r build/. /var/www/html/

            echo "Checking nginx config..."
            sudo nginx -t

            echo "Restarting nginx..."
            sudo systemctl restart nginx

            echo "Deployment completed successfully."
          EOF
